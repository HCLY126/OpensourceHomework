def user(i):
    user_str='id'
    password='password'
    if i==0:
        return user_str
    if i==1:
        return password
def user_xlsx(str):
    return str+'.xlsx'


#同目录下有成功写入excel中的例子，请使用自己账号和密码进行爬取。
